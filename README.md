# endpoints

Basic Node.js Ingestion API with:

- Express
- UUID
- Jest + Supertest
- dotenv

## Scripts

```bash
npm install
npm start
npm test
```

Then:

```bash
git add README.md
git commit -m "Add README"
git push
```
